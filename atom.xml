<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>摸鱼小窝</title>
  
  <subtitle>分享技术，资源的个人博客</subtitle>
  <link href="https://lksme.dpdns.org/atom.xml" rel="self"/>
  
  <link href="https://lksme.dpdns.org/"/>
  <updated>2026-09-11T04:05:00.000Z</updated>
  <id>https://lksme.dpdns.org/</id>
  
  <author>
    <name>凉开水</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>两个新按钮，和它们带来的三个 bug</title>
    <link href="https://lksme.dpdns.org/posts/ee8c1180.html"/>
    <id>https://lksme.dpdns.org/posts/ee8c1180.html</id>
    <published>2026-09-11T04:05:00.000Z</published>
    <updated>2026-09-11T04:05:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>这篇没什么技术含量，就是把这几天在这个站上做的事记一下。起因是有朋友说找不到夜间模式开关，我一想也是，我自己找都要找一会儿。</p><h2 id="先把夜间模式挪出来"><a href="#先把夜间模式挪出来" class="headerlink" title="先把夜间模式挪出来"></a>先把夜间模式挪出来</h2><p>主题的夜间模式按钮藏在右下角那个齿轮里，得先把鼠标移上去才露出来，露出来之后还要在一排图标里找哪一个是它。手机上没有 hover，更麻烦。</p><p>现在挪到导航栏搜索图标旁边常显了，图标跟着当前模式在月亮和太阳之间换，鼠标放上去有提示。</p><p>顺手把自动切换开了（<code>darkmode.autoChangeMode: 1</code>）：</p><ul><li>电脑或手机自己开了深色模式，进站就是深色；</li><li>系统没表态的话，晚上 18 点到早上 6 点自动切成深色；</li><li>自己手动点过之后，以点的为准，下次进站不再自动切。</li></ul><p>实现上我偷了个懒，但这个懒偷得对：切换逻辑我一行没写，只是让主题自己的按钮替我挨一下。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;darkmode&#x27;</span>).<span class="title function_">click</span>()</span><br></pre></td></tr></table></figure><p>主题那边接到点击会切 <code>data-theme</code>、弹个提示、把选择写进 localStorage。我要是再自己维护一份状态，两边迟早不一致。唯一要小心的是图标得等它改完 <code>data-theme</code> 再读，所以套了个 <code>setTimeout(syncIcon, 0)</code>。</p><h2 id="骰子按钮"><a href="#骰子按钮" class="headerlink" title="骰子按钮"></a>骰子按钮</h2><p>旁边加了个骰子，点一下随机跳一篇文章。文章清单是构建的时候生成的 <code>/posts-index.json</code>，每篇只存标题和链接，一共 5 篇。点的时候才去拉，拉过一次就放在内存里，不再重复请求。</p><p>两个小细节：随机到自己正在看的那篇会重抽，最多试 5 次；清单还没到的时候按钮透明度压到 0.5。键盘的 Enter 和空格也能按。</p><h2 id="评论表情"><a href="#评论表情" class="headerlink" title="评论表情"></a>评论表情</h2><p>评论框里加了个表情选择器，30 个，两行 emoji 加一行颜文字。</p><p>插进去的位置是光标所在处，不是直接追加到末尾。别小看这个，之前想在一句话中间插个表情，只能先删到那儿再重新打字。</p><p>这段没什么故事，唯一要想一下的是回复框是动态生成的，绑事件得绑在生成之后。</p><h2 id="然后是三个-bug"><a href="#然后是三个-bug" class="headerlink" title="然后是三个 bug"></a>然后是三个 bug</h2><h3 id="一、首页点得动，点进文章就点不动"><a href="#一、首页点得动，点进文章就点不动" class="headerlink" title="一、首页点得动，点进文章就点不动"></a>一、首页点得动，点进文章就点不动</h3><p>有朋友反馈的：晚上那个月亮图标，在首页点一下能切，点进任何一篇文章再点就没反应了。</p><p>第一反应是按钮被别的东西盖住了，调 z-index，没用。也怀疑过事件没绑上，可是首页明明能用。</p><p>真正的线索是右下角那个主题自带的夜间按钮，同一个页面、同一个功能，它一直是好的。区别在于它在 <code>#rightside</code> 里，而 <code>#rightside</code> 在 pjax 的容器外面。导航栏那一段是会被 pjax 整段换掉的。</p><p>我绑监听器的写法是：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;lks-darkmode-button&#x27;</span>).<span class="title function_">addEventListener</span>(<span class="string">&#x27;click&#x27;</span>, toggleTheme)</span><br></pre></td></tr></table></figure><p>pjax 一跳转，导航栏那段 DOM 换成新的，旧元素连同它身上的监听器一起被丢掉，新按钮上什么都没有。第一次进站是首页，页面是完整加载的，所以一切正常；只要点了站内链接，按钮就废了。</p><p>改成在 <code>document</code> 上做事件委托，转发给这个 id。以后往导航栏加东西，监听器一律绑 <code>document</code>，不绑元素本身。</p><h3 id="二、下划线歪了"><a href="#二、下划线歪了" class="headerlink" title="二、下划线歪了"></a>二、下划线歪了</h3><p>两个新按钮鼠标移上去会有一条下划线，我加完发现它比图标长，还往左偏了一截。</p><p>主题的下划线是这么写的：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.site-page</span><span class="selector-pseudo">:not</span>(<span class="selector-class">.child</span>)<span class="selector-pseudo">:after</span> &#123;</span><br><span class="line">  <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它贴着 <code>.site-page</code> 自己的框画。而我把和搜索图标之间的间距加在了 <code>.site-page</code> 上（<code>padding-left: 14px</code>），框被撑宽了 14px，下划线跟着一起变长左偏。</p><p>去看主题自己的搜索按钮怎么写的，间距是加在外层容器上的。照抄，对齐了。用无头浏览器量了一下：按钮盒子宽 18px、图标宽 18px，两者底边都在 47px。</p><h3 id="三、typed-is-not-defined"><a href="#三、typed-is-not-defined" class="headerlink" title="三、typed is not defined"></a>三、typed is not defined</h3><p>这个不是我改出来的，是主题原版的 bug，但被我新加的一条检查项抓出来了。</p><p>我在线上自检脚本里加了一项：从首页点一篇文章走 pjax，然后看控制台有没有报错。跑出来两条红的，其中一条是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ReferenceError: typed is not defined</span><br><span class="line">    at typedDestroy (utils.js:290)</span><br></pre></td></tr></table></figure><p>首页那个打字的副标题，主题注册了一个 pjax 离开页面时的清理函数，里面直接调 <code>window.typed.destroy()</code>。问题在于 <code>typed</code> 这个变量只在 <code>subtitle.effect: false</code> 的时候才会创建，而我这里配的是 <code>effect: true</code>，它压根不存在。也就是说每次从首页点进文章，控制台都会红一条，主题原版就这样，只是不一定有人看控制台。</p><p>加了个存在性判断就好了。</p><p>中间还栽在 pug 上一次：</p><details class="folding-tag" ><summary> pug 的注释坑 </summary>              <div class='content'>              <p>我改的是主题模板里的一个 <code>script.</code> 块，顺手写了行 <code>//-</code> 注释解释为什么加这个判断。结果 pug 把注释原样输出了，注释里那句 <code>{ typed.destroy() }</code> 变成了一行真的会执行的代码。</p><p><code>//-</code> 在 pug 里是模板注释，写在这个块外面没问题。写在 <code>script.</code> 里，那一段会被当成脚本内容原样吐出去。</p>              </div>            </details><h2 id="顺手发现，但这次没修"><a href="#顺手发现，但这次没修" class="headerlink" title="顺手发现，但这次没修"></a>顺手发现，但这次没修</h2><p>主题里还有一个同款问题，在处理系统深浅色切换的地方：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mediaQueryDark.<span class="title function_">addEventListener</span>(<span class="string">&#x27;change&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (saveToLocal.<span class="title function_">get</span>(<span class="string">&#x27;theme&#x27;</span>) === <span class="literal">undefined</span>) &#123;</span><br><span class="line">    e.<span class="property">matches</span> ? <span class="title function_">activateDarkMode</span>() : <span class="title function_">activateLightMode</span>()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>回调函数没接参数，里面的 <code>e</code> 是未定义的。触发条件挺具体：没手动切过模式的人，在看页面的时候系统自己切了深浅色，这时候会报错，页面也不会跟着变。我这种手动点过的反而不受影响。</p><p>修法就是加个参数，一行的事。但它属于主题文件，改完要重新打补丁、还要过一遍还原度校验，先记下来，等下次动主题的时候一起改。</p><h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><p>改的这几处都在「每页都会有」的区块里，所以外观比对会比较麻烦。用无头浏览器测了 20 项，其中一项专门测 pjax 之后点月亮还灵不灵，结果是 <code>dark -&gt; light</code>。线上自检 34 项全过，包括控制台报错那一项。</p><p>跟原站产物的逐字节还原度校验保持在 4&#x2F;8 主体一致 + 4 个登记项，退出码 0。</p><p>就这些。要是哪个按钮点不动、哪里的线歪了，或者你打开控制台看见红字，留言说一声。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;这篇没什么技术含量，就是把这几天在这个站上做的事记一下。起因是有朋友说找不到夜间模式开关，我一想也是，我自己找都要找一会儿。&lt;/p&gt;
&lt;h2 id=&quot;先把夜间模式挪出来&quot;&gt;&lt;a href=&quot;#先把夜间模式挪出来&quot; class=&quot;headerlink&quot; title=&quot;先把夜间</summary>
      
    
    
    
    <category term="折腾记录" scheme="https://lksme.dpdns.org/categories/%E6%8A%98%E8%85%BE%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="站点" scheme="https://lksme.dpdns.org/tags/%E7%AB%99%E7%82%B9/"/>
    
    <category term="Hexo" scheme="https://lksme.dpdns.org/tags/Hexo/"/>
    
  </entry>
  
  <entry>
    <title>底图、封面，还有一个转圈的 bug</title>
    <link href="https://lksme.dpdns.org/posts/93fdbfcd.html"/>
    <id>https://lksme.dpdns.org/posts/93fdbfcd.html</id>
    <published>2026-09-10T15:06:50.000Z</published>
    <updated>2026-09-11T03:59:14.789Z</updated>
    
    <content type="html"><![CDATA[<p>这周没写什么正经东西，时间基本都花在这个站上了。三件事：底图换成随机图接口、把攒的一堆图变成文章封面、修一个一直转圈的 bug。</p><h2 id="底图：从自己的图池换成别人的接口"><a href="#底图：从自己的图池换成别人的接口" class="headerlink" title="底图：从自己的图池换成别人的接口"></a>底图：从自己的图池换成别人的接口</h2><p>上个月我给站子加过”每次刷新换底图”的功能，做法是在图床里存 60 张图，前端每次随机取一张。用了一阵子发现不太行：60 张其实不多，刷几十次就开始重复，而且那批图是随机挑的，里面有几张我自己看着都别扭。</p><p>后来翻到一个二次元随机图接口，栗次元（t.alcy.cc），免费、不用注册，接口挺多：</p><table><thead><tr><th>接口</th><th>内容</th></tr></thead><tbody><tr><td><code>/ycy</code></td><td>二次元自适应（桌面横图、手机竖图自动判断）</td></tr><tr><td><code>/pc</code></td><td>PC 横图</td></tr><tr><td><code>/mp</code></td><td>手机竖图</td></tr><tr><td><code>/acg</code></td><td>ACG 动图</td></tr></tbody></table><p>我一开始想当然，以为 <code>/acg</code> 也是返回一张图片，直接当 CSS 背景写上去，测的时候背景一片黑。用 curl 看了一眼才知道它跳过去的是 mp4：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">curl -I https://t.alcy.cc/acg</span><br><span class="line"><span class="comment"># HTTP/1.1 302 Found</span></span><br><span class="line"><span class="comment"># Location: /pic/acg/83863.mp4</span></span><br></pre></td></tr></table></figure><p>一条动图 4.7 MB，塞不进 <code>background-image</code>。所以动图单独处理，在背景层里挂了个 <code>&lt;video muted loop playsinline&gt;</code>，用 <code>object-fit: cover</code> 铺满。顺手把它的抽中概率压到七分之一，不然刷几次就下 5 MB；系统开了”减少动态效果”或者浏览器开了省流量模式的时候直接跳过。</p><p>另一个坑是缓存。这几个接口都是 302 跳转，浏览器会把跳转结果缓存住 —— 我连续刷了 6 次都是同一张图，当时还以为是接口写死了。请求上带个随机参数就好了：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">https</span>:<span class="comment">//t.alcy.cc/pc?r=x8k2m1</span></span><br></pre></td></tr></table></figure><p>现在桌面端在 <code>/ycy</code>、<code>/pc</code>、<code>/acg</code> 三个接口里按权重随机挑，手机端（屏幕宽度 768 以下）固定用 <code>/mp</code> 竖图。之前那 60 张图池没删，接口出问题时会退回去用它。</p><p>调试时留了个手动开关：网址后面加 <code>?bg=pc</code> 只出横图，<code>?bg=off</code> 完全不换。</p><h2 id="封面：让新文章自己去拿一张"><a href="#封面：让新文章自己去拿一张" class="headerlink" title="封面：让新文章自己去拿一张"></a>封面：让新文章自己去拿一张</h2><p>起因是攒图。想给以后的文章留点封面素材，就往一个文件夹里丢图，断断续续丢了一百来张（后来一看 523 MB）。</p><p>本来只是放着备用，但每次写文章都要翻图、下载、转格式、传图床、再把地址粘进 front-matter，四五步，烦。索性做成一键：图丢进 <code>bg-inbox/</code>，跑一条命令，脚本转 webp、按编号传到图床、登记进封面池。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm run cover:add</span><br></pre></td></tr></table></figure><p>100 个文件里有 1 组内容完全相同（Windows 复制出来的”- 副本 - 副本”），脚本按文件内容算哈希去掉了，最后上传 97 张，一共 16 MB。</p><p>之后新建文章不用再管封面：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm run new -- <span class="string">&quot;文章标题&quot;</span></span><br></pre></td></tr></table></figure><p>建完自动从封面池里挑一张还没用过的，写进 front-matter。</p><p>这块我绕了远路。一开始想的是”构建的时候自动加”，连 front-matter 都不用写，听着比较优雅。试了三种写法：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">before_generate 里 post.cover = url</span><br><span class="line">before_generate 里 post.set(&#x27;cover&#x27;, url)</span><br><span class="line">before_post_render 里 data.cover = url</span><br></pre></td></tr></table></figure><p>日志全都打印”赋值成功”，页面渲染出来还是主题默认的那张 p17。查了半天才明白：Hexo 的文章对象对<strong>没在 schema 里声明过的字段不做持久化</strong>，我在这一处改了对象，生成阶段读出来的是另一个对象，属性没了；而且主题自己有个生成器，文章没有 cover 时会先填上默认值。</p><p>改成直接写进 front-matter，一次就对了。回头看这个选择本身也更合适：封面是什么，打开文章文件就能看见，想换随手改，不用去猜哪一层把值覆盖掉了。</p><h2 id="一个转圈的-bug"><a href="#一个转圈的-bug" class="headerlink" title="一个转圈的 bug"></a>一个转圈的 bug</h2><p>你如果前几天打开过首页，侧栏”网站信息”里有两行是转圈的：总访问量、访客数，一直转，出不来。文章页里的”阅读量”倒正常。</p><p>先怀疑接口。手动打了一下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl https://comments.lksme.dpdns.org/api/stats</span><br><span class="line">&#123;<span class="string">&quot;ok&quot;</span>:<span class="literal">true</span>,<span class="string">&quot;data&quot;</span>:&#123;<span class="string">&quot;site_pv&quot;</span>:19,<span class="string">&quot;pages&quot;</span>:5,<span class="string">&quot;site_uv&quot;</span>:6,<span class="string">&quot;today_pv&quot;</span>:19,<span class="string">&quot;comments&quot;</span>:3&#125;&#125;</span><br></pre></td></tr></table></figure><p>接口没问题。打开控制台，看到一行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[stats] 没有配置后端地址（lks_comment.api），统计与反应不启用</span><br></pre></td></tr></table></figure><p>原来是取不到后端地址，脚本直接不干活了。为什么首页取不到？因为这个地址之前是<strong>评论组件</strong>那段模板注入的，只有渲染了评论区的页面才会走到那段代码 —— 首页、归档、标签这些列表页没有评论区，自然也没有地址。文章页有评论区，所以阅读量正常。症状对上了。</p><p>改法是把地址挪到全站注入，写在 <code>scripts/inject-client-config.js</code>。这里又踩了两个坑，记一下：</p><details class="folding-tag" ><summary> 两个坑的具体样子 </summary>              <div class='content'>              <p><strong>一、取配置的时机。</strong> 最初我在注入阶段读 <code>hexo.theme.config.lks_comment</code>，拿到的是 <code>undefined</code>。主题的 <code>merge_config</code> 在 <code>before_generate</code>（优先级 1）把合并结果写进 <code>theme.config</code>，但到渲染收尾时它已经变回主题自带的 <code>_config.yml</code> 了，那里面没有 <code>lks_comment</code> 这种自定义键。所以得在 <code>before_generate</code>（优先级放到 25，晚于主题的合并）先把值缓存下来。</p><p><strong>二、<code>injector.register</code> 的求值时机。</strong> 我本来用 <code>hexo.extend.injector.register(&#39;head_end&#39;, () =&gt; ...)</code>，结果注入出来是空字符串。加了一行日志看顺序才发现：这个内容函数<strong>在注册的时候就被调用了</strong>，不是在每页渲染时，那会儿缓存还是空的。换成 <code>after_render:html</code> 过滤器逐页拼接，就正常了。</p>              </div>            </details><p>顺手把另一个情况也修了：万一接口真的挂了，或者被浏览器插件挡住，前端原来会一直转下去。现在请求失败、或者 8 秒还没结果，就写一个破折号，总比让人盯着一个转圈的图标强。</p><h2 id="零碎的"><a href="#零碎的" class="headerlink" title="零碎的"></a>零碎的</h2><ul><li>把 GitHub Actions 里的 action 升到 v7、Node 升到 24，之前每次跑都提示 Node 20 已弃用。</li><li><code>bg-inbox/</code> 里那 98 张原图现在可以删了。封面都传上去并且在 <code>source/_data/covers.yml</code> 里登记过，重复跑也不会重复加。</li><li>底图池那 60 张留在图床当兜底，暂时不打算删。</li></ul><p>就这些。要是刷页面的时候还看到哪里在转圈，留言说一声。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;这周没写什么正经东西，时间基本都花在这个站上了。三件事：底图换成随机图接口、把攒的一堆图变成文章封面、修一个一直转圈的 bug。&lt;/p&gt;
&lt;h2 id=&quot;底图：从自己的图池换成别人的接口&quot;&gt;&lt;a href=&quot;#底图：从自己的图池换成别人的接口&quot; class=&quot;headerl</summary>
      
    
    
    
    <category term="折腾记录" scheme="https://lksme.dpdns.org/categories/%E6%8A%98%E8%85%BE%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="站点" scheme="https://lksme.dpdns.org/tags/%E7%AB%99%E7%82%B9/"/>
    
    <category term="Cloudflare" scheme="https://lksme.dpdns.org/tags/Cloudflare/"/>
    
  </entry>
  
  <entry>
    <title>留言板终于能留言了：自建评论系统记录</title>
    <link href="https://lksme.dpdns.org/posts/2dddc10c.html"/>
    <id>https://lksme.dpdns.org/posts/2dddc10c.html</id>
    <published>2026-09-10T09:45:00.000Z</published>
    <updated>2026-09-11T03:59:14.789Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>想说的话，总得有个地方落下来。</p></blockquote><p>留言板挂了很久了 —— 准确说，它一直”看起来”是好的：页面打开有个挺好看的信封，写着「来自凉开水的留言」，就是<strong>没有任何地方能输入</strong>。这篇记录一下我把它彻底修好、并且顺手把评论数据揣回自己兜里的过程。</p><h2 id="一、先说结论"><a href="#一、先说结论" class="headerlink" title="一、先说结论"></a>一、先说结论</h2><div class="note info flat"><p>现在留言板、每篇文章底部都有评论框了。<strong>不需要登录任何账号</strong>，填个昵称就能发；数据存在我自己的 Cloudflare 数据库里，不经过第三方评论服务。</p><p>后端是一个自己写的小 Worker，代码就在博客仓库的 <code>comment-worker/</code> 目录里 —— 换句话说，这套东西的源码和文章放在一起，不会再有”服务商跑路我就抓瞎”的问题。</p></div><h2 id="二、为什么留言板留不了言"><a href="#二、为什么留言板留不了言" class="headerlink" title="二、为什么留言板留不了言"></a>二、为什么留言板留不了言</h2><p>排查下来是两个原因叠在一起：</p><ol><li><strong>那个信封是纯装饰。</strong> <code>/comments/</code> 页面是 <code>hexo-butterfly-envelope</code> 插件生成的，我翻了插件源码，它渲染出来的表单长这样：</li></ol><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;formmain&quot;</span> <span class="attr">style</span>=<span class="string">&quot;pointer-events: none;&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure><p><code>pointer-events: none</code> —— 整个表单不可交互，它只负责画个信封、写几句”有什么想说的”。</p><ol start="2"><li><strong>真正的评论框没被启用。</strong> 能输入、能提交的评论框由主题的评论系统渲染，而配置文件里 <code>comments.use</code> 是<strong>空的</strong>。没有评论系统 → 没有评论框 → 自然也谈不上后端。</li></ol><p>所以严格说，留言板不是”坏了”，是<strong>从来没接上过</strong>。</p><h2 id="三、试过的三条路，都不太顺"><a href="#三、试过的三条路，都不太顺" class="headerlink" title="三、试过的三条路，都不太顺"></a>三、试过的三条路，都不太顺</h2><p>静态博客自己存不了数据，”能留言”靠的是把数据存到别处。我先试了三条现成的路：</p><table><thead><tr><th align="left">方案</th><th align="left">结果</th></tr></thead><tbody><tr><td align="left">Waline + 腾讯云 CloudBase 一键部署</td><td align="left">✗ 我这边的腾讯云环境建不了 CloudBase，那个”一键部署”按钮点下去走不通</td></tr><tr><td align="left">Waline + Vercel + Neon 数据库</td><td align="left">✗ 要注册两个账号，而且 Vercel 在国内访问一般</td></tr><tr><td align="left">Giscus &#x2F; Utterances（基于 GitHub）</td><td align="left">✗ 零后端确实最省事，但<strong>留言的人必须登录 GitHub</strong> —— 普通朋友基本不会为留一句话去注册账号</td></tr></tbody></table><p>Waline 官方也不支持部署到 Cloudflare Workers，那条捷径也堵着。</p><h2 id="四、最后为什么自己写"><a href="#四、最后为什么自己写" class="headerlink" title="四、最后为什么自己写"></a>四、最后为什么自己写</h2><p>绕一圈发现，我缺的其实不是”评论系统”，是<strong>一个能存数据的地方</strong>。而这个地方我早就有了：</p><div class="note info flat"><p>图床 <code>pic-bed-e0x.pages.dev</code> 就跑在 Cloudflare Pages 上 —— 说明我本来就有 Cloudflare 账号。</p><p>那就用 <strong>Cloudflare Workers + D1</strong>（D1 是 Cloudflare 的 SQL 数据库）：<br>免费额度每天 10 万次请求、5GB 存储，个人博客连零头都用不到；不用备案、不用买服务器；而且访客<strong>匿名留言</strong>，不需要注册任何账号。</p></div><p>前端放在博客里（<code>source/js/comment.js</code>），后端是一个单独的 Worker，两边靠 HTTP 接口说话。</p><h2 id="五、它是怎么工作的"><a href="#五、它是怎么工作的" class="headerlink" title="五、它是怎么工作的"></a>五、它是怎么工作的</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">访客浏览器</span><br><span class="line">   │</span><br><span class="line">   │  ① 打开文章 / 留言板页</span><br><span class="line">   ├──► GitHub Pages（静态页面，评论框只是个空壳）</span><br><span class="line">   │</span><br><span class="line">   │  ② comment.js 往空壳里渲染表单 + 拉评论列表</span><br><span class="line">   ├──► Cloudflare Worker  https://comments.lksme.dpdns.org</span><br><span class="line">   │        │</span><br><span class="line">   │        └──► D1 数据库（评论、浏览量、点赞都在这里）</span><br><span class="line">   │</span><br><span class="line">   └──► ③ 填昵称 + 内容 → 提交 → 评论出现在列表里</span><br></pre></td></tr></table></figure><p>接口不多，够用就行：</p><table><thead><tr><th align="left">接口</th><th align="left">干什么</th></tr></thead><tbody><tr><td align="left"><code>GET /api/comments?path=...</code></td><td align="left">取某篇文章的评论（只返回审核通过的）</td></tr><tr><td align="left"><code>POST /api/comments</code></td><td align="left">发一条评论，支持回复</td></tr><tr><td align="left"><code>GET /api/count</code>、<code>GET /api/recent</code></td><td align="left">评论数、全站最新评论（侧边栏那张卡片）</td></tr><tr><td align="left"><code>DELETE /api/comments/:id</code></td><td align="left">删除（软删，需要管理口令）</td></tr><tr><td align="left"><code>GET /admin</code></td><td align="left">一个简易管理页面：搜索、删除、通过待审核</td></tr></tbody></table><p>数据库五张表，各管一摊：</p><table><thead><tr><th align="left">表</th><th align="left">存什么</th></tr></thead><tbody><tr><td align="left"><code>comments</code></td><td align="left">评论本体（含 <code>status</code>：正常 &#x2F; 待审核 &#x2F; 已删除）</td></tr><tr><td align="left"><code>pageviews</code> + <code>pv_log</code></td><td align="left">每篇文章的浏览量 + 去重日志</td></tr><tr><td align="left"><code>reactions</code> + <code>reaction_log</code></td><td align="left">文章点赞数 + 去重日志</td></tr></tbody></table><h3 id="两个小设计"><a href="#两个小设计" class="headerlink" title="两个小设计"></a>两个小设计</h3><p><strong>去重</strong>：同一 IP 对同一篇文章，浏览量<strong>每小时只算一次</strong>、同一种点赞<strong>每天只算一次</strong> —— 免得有人按 F5 把数字顶上天。</p><p><strong>隐私</strong>：数据库里<strong>不存明文 IP</strong>，只存 <code>sha256(IP + 随机盐)</code> 的前 32 位，用途仅限限流；邮箱只有我自己在管理后台看得到，前台接口根本不返回这个字段。</p><h2 id="六、防刷四件套"><a href="#六、防刷四件套" class="headerlink" title="六、防刷四件套"></a>六、防刷四件套</h2><p>留言板一旦公开，最先来的往往是广告机器人。现在有四层：</p><table><thead><tr><th align="left">手段</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">蜜罐字段</td><td align="left">表单里有个真人看不见的输入框，机器人爱填 —— 填了就假装成功、实际丢弃</td></tr><tr><td align="left">限流</td><td align="left">同一 IP 每分钟 1 条、每天 20 条</td></tr><tr><td align="left">含链接进待审核</td><td align="left">带链接的评论先隐藏，我在后台点了”通过”才显示（广告九成都带链接）</td></tr><tr><td align="left">Turnstile（可选）</td><td align="left">Cloudflare 的免费人机验证，需要时在配置里填个 sitekey 就会出现</td></tr></tbody></table><h2 id="七、顺手做的两件事"><a href="#七、顺手做的两件事" class="headerlink" title="七、顺手做的两件事"></a>七、顺手做的两件事</h2><p>既然后端有了，同一套东西还能干点别的：</p><p><strong>① 自己统计浏览量。</strong> 原来用的是 busuanzi，但它的域名在国内经常超时，数字时有时无。现在换成自己的接口了。</p><details class="folding-tag" ><summary> 这里有个取巧但挺妙的处理 </summary>              <div class='content'>              <p>busuanzi 在页面上的显示位置，主题是用固定 id 写死的：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">&quot;busuanzi_value_site_pv&quot;</span>&gt;</span>…<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">&quot;busuanzi_value_page_pv&quot;</span>&gt;</span>…<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br></pre></td></tr></table></figure><p>关掉 busuanzi 插件会让这些元素消失，页面结构就跟原来不一样了。所以我<strong>没改主题模板</strong>，而是把 busuanzi 的脚本换成一个空文件，元素 id 保持原样，再让自己写的统计脚本往同样的元素里填数据 —— 页面结构不变、只有一个数据写入方，两套数字不会打架。</p>              </div>            </details><p><strong>② 文章点赞。</strong> 文章底部多了三个按钮：👍 有用 &#x2F; 💡 学到了 &#x2F; ❤️ 喜欢。留评论的门槛比点赞高得多，这个基本是顺手点的。</p><h2 id="八、踩过的坑"><a href="#八、踩过的坑" class="headerlink" title="八、踩过的坑"></a>八、踩过的坑</h2><details class="folding-tag" ><summary> 本地起 Waline 服务端做测试，卡死 </summary>              <div class='content'>              <p>想在本地先跑通一遍再动线上，结果 <code>wrangler dev</code> 里那套 Waline 服务端在 Node 26 上直接卡住不响应 —— 追下去是它依赖的原生 SQLite 模块没有 Node 26 的预编译包。</p><p>后来想通了：<strong>我本来就不需要本地跑 Waline</strong>，我用的是自己写的 Worker，本地用 wrangler 自带的 D1 模拟就能测，没必要绕那一圈。</p>              </div>            </details><details class="folding-tag" ><summary> 侧边栏「最新评论」卡片一直显示"加载中" </summary>              <div class='content'>              <p>主题自带的几个评论后端，都是把初始化挂在 <code>window</code> 的 <code>load</code> 事件上。而页面里有一堆 CDN 资源，在国内经常加载得很慢 —— <code>load</code> 事件被拖到十几秒甚至更久才触发，卡片就一直停在”加载中”。</p><p>改成”DOM 就绪就执行”就好了。顺带把这习惯带进了自己的脚本：<strong>能用 DOMContentLoaded 就别等 load。</strong></p>              </div>            </details><details class="folding-tag" ><summary> 测试自己的限流把测试自己挡住了 </summary>              <div class='content'>              <p>写自动化测试的时候连着发两条评论，第二条被限流挡了 —— 因为限流是”每分钟 1 条”。测试里手动放宽了限制才跑通。</p><p>这个坑提醒我：<strong>限流这类策略要能配置</strong>，不然测试和维护都会很别扭。</p>              </div>            </details><details class="folding-tag" ><summary> 假失败：国内网络下"页面没加载完"被我判成了"功能坏了" </summary>              <div class='content'>              <p>线上自检脚本原来固定等 6 秒就下结论，结果经常报”评论列表是空的” —— 其实只是页面 JS 还没跑起来。</p><p>改成轮询等待（最多 45 秒）之后就稳了。<strong>别用固定 sleep 判断异步结果</strong>，这是我这轮学到最实用的一条。</p>              </div>            </details><h2 id="九、成本、备份和管理"><a href="#九、成本、备份和管理" class="headerlink" title="九、成本、备份和管理"></a>九、成本、备份和管理</h2><ul><li><strong>成本</strong>：目前是 0 元。免费额度是每天 10 万次请求，我这个量级大概用掉几百分之一。</li><li><strong>备份</strong>：评论都在 Cloudflare 的 D1 里，一条命令导出：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npx wrangler d1 <span class="built_in">export</span> lks-blog-comment --remote --output=comments-backup.sql</span><br></pre></td></tr></table></figure><ul><li><strong>管理</strong>：打开 <code>https://comments.lksme.dpdns.org/admin</code>，输管理口令就能删评论、通过待审核、按关键词搜索。</li><li><strong>换域名不影响评论</strong>：评论是按页面路径存的，跟域名无关。</li></ul><h2 id="十、写在最后"><a href="#十、写在最后" class="headerlink" title="十、写在最后"></a>十、写在最后</h2><p>现在留言板是通的：<strong>不用登录、不用等审核（不带链接的话）、填个昵称就能说话</strong>。</p><p>技术上没什么高深的东西，就是一个 Worker 加五张表。真正的收获是两件事：</p><ol><li><strong>遇到”现成方案都用不了”的时候，先想清楚自己缺的到底是什么</strong> —— 我缺的只是”一个能存数据的地方”，而不是”一个评论系统”。</li><li><strong>静态站不等于没有动态能力</strong>。静态只是说页面是预先生成好的，它照样可以调用接口、写数据库、做点赞和统计。</li></ol><p>要是你在留言时发现哪里不对劲，欢迎就地吐槽 —— 通道现在是通的 😉</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;想说的话，总得有个地方落下来。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;留言板挂了很久了 —— 准确说，它一直”看起来”是好的：页面打开有个挺好看的信封，写着「来自凉开水的留言」，就是&lt;strong&gt;没有任何地方能输入&lt;/strong&gt;。这篇记录</summary>
      
    
    
    
    <category term="折腾记录" scheme="https://lksme.dpdns.org/categories/%E6%8A%98%E8%85%BE%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="站点" scheme="https://lksme.dpdns.org/tags/%E7%AB%99%E7%82%B9/"/>
    
    <category term="Cloudflare" scheme="https://lksme.dpdns.org/tags/Cloudflare/"/>
    
    <category term="评论系统" scheme="https://lksme.dpdns.org/tags/%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>源码丢了又找回来了：本站近期折腾记录</title>
    <link href="https://lksme.dpdns.org/posts/6d19d5ca.html"/>
    <id>https://lksme.dpdns.org/posts/6d19d5ca.html</id>
    <published>2026-09-09T15:10:00.000Z</published>
    <updated>2026-09-11T03:59:14.789Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>生活明朗，万物可爱。</p></blockquote><p>好久没写新东西了。这两个月没更新文章，倒是把整个小窝从里到外翻修了一遍 —— 起因挺离谱的：<strong>源码丢了。</strong></p><h2 id="一、事故现场"><a href="#一、事故现场" class="headerlink" title="一、事故现场"></a>一、事故现场</h2><p>某天想改点东西，打开 GitHub 仓库一看，人有点懵：</p><div class="note warning flat"><p>仓库里躺着 34 次提交，全都叫 <code>Site updated: xxxx-xx-xx</code>，一个字的源码都没有。</p></div><p>原因其实特别常见：<code>hexo-deployer-git</code> 推上去的永远是 <code>public/</code> 里的<strong>构建产物</strong>，源码从来只活在本地。本地那份一丢，就真没了。</p><p>当时摆在面前的三条路：</p><table><thead><tr><th align="left">方案</th><th align="left">可行性</th></tr></thead><tbody><tr><td align="left">从 GitHub 找回源码</td><td align="left">✗ 仓库里只有产物，也没有第二个分支</td></tr><tr><td align="left">从本机 &#x2F; 回收站找回</td><td align="left">✗ 全盘搜过一遍，没有 <code>_config.yml</code>，也没有 <code>themes/</code></td></tr><tr><td align="left"><strong>从构建产物反向重建</strong></td><td align="left">✓ 可行，而且能做得几乎一模一样</td></tr></tbody></table><h2 id="二、重建是怎么做的"><a href="#二、重建是怎么做的" class="headerlink" title="二、重建是怎么做的"></a>二、重建是怎么做的</h2><p>好在 Hexo 的产物信息量很大，相当于把答案抄在了卷子上：</p><div class="note info flat"><p><code>&lt;meta name=&quot;generator&quot;&gt;</code> 直接给出 Hexo 版本；主题的 <code>js/main.js</code> 哈希能定位主题版本；<br>编译后的 <code>css/index.css</code> 里，所有配置项生效后的结果都看得见。</p></div><p>大致的流程是：</p><ol><li>克隆线上仓库，把 90 个产物文件全部拆开看一遍</li><li>拿 <code>js/main.js</code> 的 SHA256 去比对主题的每一个 tag，锁定到 <strong>Butterfly 5.3.3</strong></li><li>从编译后的 CSS 反推配置（配色、字号、代码块主题、加载动画……）</li><li>按 5.3.3 装好主题、把配置写回去，然后<strong>逐文件 diff</strong>，差什么补什么</li></ol><p>后来翻云盘，还翻出一份 2025 年 2 月的源码备份，里面有真实的 <code>_config.yml</code>、<code>package.json</code> 和 <code>package-lock.json</code>。插件版本一按 lock 锁死，产物立刻就对上了。</p><details class="folding-tag" ><summary> 看一眼还原度校验的结果 </summary>              <div class='content'>              <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">逐字节比对    ： 39 / 67 完全一致</span><br><span class="line">HTML 主体比对 ：  8 /  8 完全一致（已排除插件注入块）</span><br></pre></td></tr></table></figure><p>剩下的差异来自构建时间戳、页脚年份、域名，以及后来<strong>故意</strong>改掉的东西（新文章、新脚本）。这个校验脚本留在仓库里了，<code>npm run verify</code> 随时能跑。</p>              </div>            </details><h2 id="三、顺手修好的一堆东西"><a href="#三、顺手修好的一堆东西" class="headerlink" title="三、顺手修好的一堆东西"></a>三、顺手修好的一堆东西</h2><p>重建完才发现，原来有些东西<strong>早就坏了</strong>，只是一直没人注意：</p><table><thead><tr><th align="left">问题</th><th align="left">真正的原因</th><th align="left">状态</th></tr></thead><tbody><tr><td align="left">音乐播放器不见了</td><td align="left">MetingJS 的默认接口已经挂了</td><td align="left"><mark class="hl-label green">已修复</mark></td></tr><tr><td align="left">侧边栏电子钟一片空白</td><td align="left">天气 key 后面跟着注释、API Host 没配</td><td align="left"><mark class="hl-label green">已修复</mark></td></tr><tr><td align="left">电子钟永远显示长沙</td><td align="left">高德 IP 定位只认 IPv4，而浏览器走的是 IPv6</td><td align="left"><mark class="hl-label green">已修复</mark></td></tr><tr><td align="left">加载动画只有个空圆框</td><td align="left">头像图片路径写错了，一直 404</td><td align="left"><mark class="hl-label green">已修复</mark></td></tr></tbody></table><p>一个一个说。</p><h3 id="🎵-音乐播放器"><a href="#🎵-音乐播放器" class="headerlink" title="🎵 音乐播放器"></a>🎵 音乐播放器</h3><p>播放器是靠 APlayer + MetingJS 取网易云歌单的，而 MetingJS 默认调用的<br><code>api.i-meto.com</code> 这个接口<strong>已经失效了</strong>。取不到歌单，APlayer 干脆整个不渲染 —— 表现就是”播放器凭空消失”。</p><p>换成可用接口后就好了：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">data-api=&quot;https://api.qijieya.cn/meting/?server=:server&amp;type=:type&amp;id=:id&amp;r=:r&quot;</span><br></pre></td></tr></table></figure><div class="note info flat"><p>这类公共接口都不太稳定，挂了就换一个。想要一劳永逸，可以自己部署一份 Meting API 到 Cloudflare Workers 或 Vercel。</p></div><h3 id="⏰-电子钟：从”一片空白”到”显示错城市”"><a href="#⏰-电子钟：从”一片空白”到”显示错城市”" class="headerlink" title="⏰ 电子钟：从”一片空白”到”显示错城市”"></a>⏰ 电子钟：从”一片空白”到”显示错城市”</h3><p>电子钟的渲染逻辑是<strong>先取天气、再画时钟</strong> —— 天气接口一失败，整张卡片就是空白，看着像时钟坏了。</p><p>原配置里踩了两个坑：</p><ul><li><code>qweather_key</code> 后面跟了一串注释（<code># 和风天气key</code>），被一起拼进了请求 URL；</li><li>没配 <code>qweather_api_host</code>，于是回退到插件作者自己的域名，而那个域名只认作者本人的 key。</li></ul><p>两个都会拿到 <code>403 Invalid Host</code>。清理之后又发现新问题：<strong>永远显示长沙</strong>。</p><p>查下来是个很隐蔽的坑：</p><div class="note warning flat"><p>高德的 IP 定位接口明确写着「仅支持 IPV4，不支持国外 IP 解析」，而 <code>restapi.amap.com</code><br>同时有 IPv6 地址。访客浏览器优先走 IPv6 去请求，高德就直接返回空数组。</p></div><p>同一台机器实测：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -4 <span class="string">&quot;https://restapi.amap.com/v3/ip?key=xxx&quot;</span>   <span class="comment"># → 河南省 / 安阳市 ✔</span></span><br><span class="line">curl -6 <span class="string">&quot;https://restapi.amap.com/v3/ip?key=xxx&quot;</span>   <span class="comment"># → province:[], city:[], rectangle:[] ✗</span></span><br></pre></td></tr></table></figure><p>所以现在的做法是：<strong>先取访客的 IPv4，再带 <code>?ip=</code> 参数调高德</strong>，后面还有两层兜底。切走再回来，Console 里能看到这样一行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[clock] 定位来源：高德 → 安阳市（114.346175,36.100128）</span><br></pre></td></tr></table></figure><h3 id="🖼️-加载动画"><a href="#🖼️-加载动画" class="headerlink" title="🖼️ 加载动画"></a>🖼️ 加载动画</h3><p>打开网页时中间那个圆形头像，其实一直是空的 —— <code>loading.css</code> 里图片路径写成了 <code>/source/img/p18.webp</code>，<br>这个路径不存在（404），而图片真正的家在 <code>/img/p18.webp</code>。改一个字符就好了。</p><h2 id="四、新增的小彩蛋"><a href="#四、新增的小彩蛋" class="headerlink" title="四、新增的小彩蛋"></a>四、新增的小彩蛋</h2><h3 id="标签页标题"><a href="#标签页标题" class="headerlink" title="标签页标题"></a>标签页标题</h3><p>现在切走标签页，标题会变成：</p><blockquote><p>这就要走了吗 (｡•́︿•̀｡)</p></blockquote><p>而且每 4 秒换一句（不会连续重复）。切回来的瞬间是：</p><blockquote><p>你终于回来了 (ﾉ´ヮ&#96;)ﾉ*: ･ﾟ</p></blockquote><p>3 秒后自动恢复成正常的站点标题。</p><h3 id="首页布局"><a href="#首页布局" class="headerlink" title="首页布局"></a>首页布局</h3><p>以前打开首页是整屏底图，一篇文章都看不见。现在头图只占半屏，下半屏直接就是文章列表：</p><table><thead><tr><th align="left"></th><th align="left">改前</th><th align="left">改后</th></tr></thead><tbody><tr><td align="left">头图高度</td><td align="left">900px（占满首屏）</td><td align="left">450px（半屏）</td></tr><tr><td align="left">文章列表起始</td><td align="left">940px</td><td align="left">490px</td></tr></tbody></table><h2 id="五、现在怎么维护"><a href="#五、现在怎么维护" class="headerlink" title="五、现在怎么维护"></a>五、现在怎么维护</h2><details class="folding-tag" ><summary> 常用命令 </summary>              <div class='content'>              <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">npm run new -- <span class="string">&quot;文章标题&quot;</span>   <span class="comment"># 写新文章（自动生成短链）</span></span><br><span class="line">npm run rebuild            <span class="comment"># 清缓存后全量重建</span></span><br><span class="line">npm run verify             <span class="comment"># 校验构建产物与线上原站的还原度</span></span><br><span class="line">npm <span class="built_in">test</span>                   <span class="comment"># 跑回归测试（电子钟 / 标签页彩蛋）</span></span><br><span class="line">npm run deploy             <span class="comment"># 部署到 GitHub Pages</span></span><br></pre></td></tr></table></figure>              </div>            </details><p>还给时钟脚本和标签页彩蛋各写了一套回归测试（用假 DOM + 可控时钟跑的，不用开浏览器），以后改坏了会立刻发现。</p><h2 id="六、写在最后"><a href="#六、写在最后" class="headerlink" title="六、写在最后"></a>六、写在最后</h2><p>折腾这一圈最大的感受是：<strong>构建产物也是源码</strong>。只要产物还在，很多东西都能反推回来 —— 当然，还是别赌这个，源码记得多存一份。</p><p>顺便说一句，现在这篇是重建之后的第一篇文章，能正常发出来，说明这条流水线是通的 🎉</p><p>如果发现哪里还是坏的、或者有什么想让我加的小玩意，欢迎在<a href="/comments/">留言板</a>说一声。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;生活明朗，万物可爱。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;好久没写新东西了。这两个月没更新文章，倒是把整个小窝从里到外翻修了一遍 —— 起因挺离谱的：&lt;strong&gt;源码丢了。&lt;/strong&gt;&lt;/p&gt;
&lt;h2 id=&quot;一、事故现场&quot;&gt;&lt;a </summary>
      
    
    
    
    <category term="折腾记录" scheme="https://lksme.dpdns.org/categories/%E6%8A%98%E8%85%BE%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="站点" scheme="https://lksme.dpdns.org/tags/%E7%AB%99%E7%82%B9/"/>
    
    <category term="hexo" scheme="https://lksme.dpdns.org/tags/hexo/"/>
    
  </entry>
  
  <entry>
    <title>添加侧边栏电子钟</title>
    <link href="https://lksme.dpdns.org/posts/4ce3e802.html"/>
    <id>https://lksme.dpdns.org/posts/4ce3e802.html</id>
    <published>2025-04-12T03:33:50.000Z</published>
    <updated>2025-04-12T04:24:18.147Z</updated>
    
    <content type="html"><![CDATA[<h1 id="添加侧边栏电子钟"><a href="#添加侧边栏电子钟" class="headerlink" title="添加侧边栏电子钟"></a>添加侧边栏电子钟</h1><p>建设中…</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;添加侧边栏电子钟&quot;&gt;&lt;a href=&quot;#添加侧边栏电子钟&quot; class=&quot;headerlink&quot; title=&quot;添加侧边栏电子钟&quot;&gt;&lt;/a&gt;添加侧边栏电子钟&lt;/h1&gt;&lt;p&gt;建设中…&lt;/p&gt;
</summary>
      
    
    
    
    <category term="折腾记录" scheme="https://lksme.dpdns.org/categories/%E6%8A%98%E8%85%BE%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="hexo" scheme="https://lksme.dpdns.org/tags/hexo/"/>
    
    <category term="butterfly" scheme="https://lksme.dpdns.org/tags/butterfly/"/>
    
    <category term="美化" scheme="https://lksme.dpdns.org/tags/%E7%BE%8E%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>关于本站</title>
    <link href="https://lksme.dpdns.org/posts/8b8a8c83.html"/>
    <id>https://lksme.dpdns.org/posts/8b8a8c83.html</id>
    <published>2025-02-02T12:00:00.000Z</published>
    <updated>2025-04-12T04:24:23.332Z</updated>
    
    <content type="html"><![CDATA[<h1 id="关于本站"><a href="#关于本站" class="headerlink" title="关于本站"></a>关于本站</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>本站使用 <a href="https://hexo.io/">Hexo</a> 搭建，主题为 <a href="https://github.com/jerryc127/hexo-theme-butterfly">butterfly</a>。托管于 <a href="https://github.com/liang-kai-shui/liang-kai-shui.github.io">GitHub</a>。</p><h2 id="感谢"><a href="#感谢" class="headerlink" title="感谢"></a>感谢</h2><ul><li><a href="https://github.com/jerryc127/hexo-theme-butterfly">butterfly</a></li><li><a href="https://hexo.io/">Hexo</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;关于本站&quot;&gt;&lt;a href=&quot;#关于本站&quot; class=&quot;headerlink&quot; title=&quot;关于本站&quot;&gt;&lt;/a&gt;关于本站&lt;/h1&gt;&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h</summary>
      
    
    
    
    <category term="站点" scheme="https://lksme.dpdns.org/categories/%E7%AB%99%E7%82%B9/"/>
    
    
    <category term="站点" scheme="https://lksme.dpdns.org/tags/%E7%AB%99%E7%82%B9/"/>
    
  </entry>
  
</feed>
